# Production Requirements for Railway
# Optimized to reduce image size while keeping ALL core functionality
# chromadb + sentence-transformers are REQUIRED for schema intelligence

# Core Dependencies
google-generativeai>=0.3.0
pyyaml>=6.0
jsonschema>=4.0.0
python-dotenv>=1.0.0

# Database
duckdb>=0.9.0

# Vector Store & Embeddings (CORE - DO NOT REMOVE)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Google Sheets Integration
gspread>=5.0.0
google-auth>=2.0.0

# Google Cloud Translation API
google-cloud-translate>=3.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Voice & Audio (Production)
elevenlabs>=1.0.0
langdetect>=1.0.9

# Utilities
requests>=2.31.0

# Authentication
supabase>=2.0.0

# API Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ============================================
# REMOVED (Not used in production FastAPI):
# ============================================
# streamlit>=1.28.0          - Only used in legacy auth functions (optional imports)
# gtts>=2.4.0                - Not used anywhere in codebase
# pydub>=0.25.1              - Not used anywhere in codebase  
# scipy>=1.10.0              - Not used anywhere in codebase
